# https://taskfile.dev

version: "3"

vars:
  GREETING: Hello, World!

tasks:
  _install_rust:
    internal: true
    status:
      - rustc --version
    cmds:
      - curl https://sh.rustup.rs -sSf | sh

  build:
    deps:
      - _install_rust
    cmds:
      - cargo build
